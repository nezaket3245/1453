# Cloudflare Pages Redirects
# www to non-www redirect
https://www.akcapen-pvc.pages.dev/* https://akcapen-pvc.pages.dev/:splat 301

# HTTP to HTTPS (handled automatically by Cloudflare)

# Old URL redirects (if any)
/index.html / 301
/home / 301

# Trailing slash normalization
/*/ /:splat 301
